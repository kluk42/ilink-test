import {Props} from './types';

import {Button} from './styles';

const CheckBtn: Props = () => {
  return (
        <Button
            active={false}
        >
            Check
        </Button>
  );
}

export default CheckBtn;
